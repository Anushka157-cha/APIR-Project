# Demo

Start Compose, sign in as SRE, confirm services on the dashboard, start `FAIL_PAYMENT_LATENCY`, send order traffic to the gateway, inspect the detected incident, approve the plan, then observe multi-sample verification and evaluation results.
