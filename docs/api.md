# API

Authenticated API highlights: `GET /services`, `GET /incidents`, `POST /incidents/{id}/investigate`, `POST /remediation/{plan}/approve`, `GET /rag/search`, `POST /rag/ingest`, `GET /evaluation`, `POST /failures/{scenario}/start`, and `POST /replay/{scenario}`. SRE and ADMIN roles can use operational endpoints; action-specific permissions are also checked.
